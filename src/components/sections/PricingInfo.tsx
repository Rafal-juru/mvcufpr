/* PricingInfo.tsx — stub com fundo cesmvc-blue obrigatório (Etapa 2) */
export default function PricingInfo() {
  return (
    <section id="pricing" className="py-24 sm:py-32 bg-cesmvc-blue">
      <div className="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <span className="inline-flex items-center gap-2 text-white/60 text-xs font-bold tracking-widest uppercase mb-4">
          <span className="w-8 h-px bg-white/40 inline-block" />
          Investimento
        </span>
        <h2 className="font-grift text-white text-3xl sm:text-4xl font-bold mb-4">
          Valores e Formas de Pagamento
        </h2>
        <p className="text-white/60 text-base">
          [Componente PricingInfo.tsx — tabela de preços será implementada na Etapa 2]
        </p>
      </div>
    </section>
  )
}
