node_modules/
.env
.env.local
*.log
.DS_Store

# Imagens enviadas em runtime — não versionar (manter só a pasta via .gitkeep)
uploads/*
!uploads/.gitkeep

# Build do frontend copiado para cá no deploy (gerado por `npm run build:deploy`)
public/
