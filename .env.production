# Carregado automaticamente pelo `vite build` (modo production).
# API e frontend ficam no MESMO domínio (Node/Express serve os dois),
# então a base da API é relativa (/api) — VITE_API_URL fica vazio.
VITE_USE_MOCK=false
VITE_API_URL=
